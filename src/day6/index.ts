import { DayFunction } from "../utilities";

const dayFunction: DayFunction = (input: string[]) => {
  const initialCounts = input[0].split(",");
  console.log(initialCounts);
  return;
};

export default dayFunction;
